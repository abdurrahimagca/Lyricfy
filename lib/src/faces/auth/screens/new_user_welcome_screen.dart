import 'package:flutter/widgets.dart';

class NewUserWelcomeScreen extends StatelessWidget {
  const NewUserWelcomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text("Welcome to the app!"),
    );
  }
}
